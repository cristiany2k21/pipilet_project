import React from 'react';

const Description = ({
  description
}) => {
    return (
        <div className="wrapper-description">
          {description}
        </div>
    )
};

export default Description;