import React from 'react';

const BigDraw = ({
	                 draw
                 }) => {
	return (
	<div className="wrapper-big-draw">
		<img src={draw}alt="logo" />
	</div>
	);
};

export default BigDraw;
