import React from 'react';

const BigDraw = ({
	                 draw
                 }) => {
	return (
	<div className="wrapper-big-draw">
		<img src={draw}/>
	</div>
	);
};

export default BigDraw;
